container_commands:
  01_clear_pip_cache:
    command: "rm -rf /root/.cache/pip || true"

  02_install_requirements_no_cache:
    command: "/var/app/venv/*/bin/pip install --no-cache-dir -r requirements.txt"
